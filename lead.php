<?php
// The landing.html booking form posts here (its endpoint from the current build).
// Delegate to the single mailer one level up so there is one source of truth.
require dirname(__DIR__) . '/submit.php';
